# config.py

# CNN model path
CNN_MODEL_PATH = "models/fingerprint_cnn_model.keras"

# Expected input image size (must match the training size of your CNN)
IMAGE_SIZE = (128, 128)

# List of class labels (blood groups)
CLASSES = ['A', 'B', 'AB', 'O']
